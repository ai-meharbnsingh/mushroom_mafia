# =============================================================
# FRONTEND — LOCAL DEVELOPMENT
# =============================================================
# Copy to .env:  cp .env.example .env
# No changes needed if backend runs on localhost:3800.
# =============================================================

VITE_API_BASE_URL=http://localhost:3800/api/v1
VITE_WS_BASE_URL=ws://localhost:3800/api/v1
